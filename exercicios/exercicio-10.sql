SELECT *
FROM Person.Person
WHERE FirstName LIKE 'Ovi%';

--

SELECT *
FROM Person.Person
WHERE LastName LIKE '%to';

--

SELECT *
FROM Person.Person
WHERE LastName LIKE '%essa%';

--

SELECT *
FROM Person.Person
WHERE LastName LIKE '%ro_'
